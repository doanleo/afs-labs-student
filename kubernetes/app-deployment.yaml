apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
  namespace: ldoan
  labels:
    app: app
spec:
  replicas: 1
  strategy:
    type: Recreate
  minReadySeconds: 20
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - envFrom:
        - secretRef:
            name: db-secret
        name: app-deployment
        image: registry.dev.afsmtddso.com/ldoan-harbor-project/app:a72dd07-143f14f
        ports:
        - containerPort: 5000
